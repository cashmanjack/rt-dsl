[package]
name = "gprt-codegen"
version = "0.1.0"
edition = "2024"

[dependencies]
gprt-ir = { path = "../gprt-ir" }
syn = { version = "2.0", features = ["full", "extra-traits", "visit"] }
quote = "1.0"
